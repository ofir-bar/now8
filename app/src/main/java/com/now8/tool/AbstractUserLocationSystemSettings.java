package com.now8.tool;

// This abstract class deals with required device system configurations(e.g. GPS is turned on), which are essential for location requests
public abstract class AbstractUserLocationSystemSettings {
}
